# tools/versions.env
# Pinned versions for reproducible OurBox Mini OS builds.
# Change ONLY deliberately, and update docs/OPS.md "Last verified" after re-verifying end-to-end.

K3S_VERSION=v1.35.0+k3s1

# Demo workload baked into the image as an offline example.
NGINX_IMAGE=nginx:1.27-alpine

# BuildKit version installed by tools/bootstrap-host.sh (host-side)
BUILDKIT_VERSION=v0.23.2
